// use crate::cli;
// use crate::frontend;
// use crate::middleend;
// // use crate::backend;

// pub fn compile() -> Result<_, UmError> { 
//   let mut config = cli::get_config_from_cli();
//   let mut connection = middleend::ir_setup::setup_ir_connection().unwrap(); // TODO: replace with ?

//   // frontend::run(&connection, &config)?;
//   // backend::run(&connection, &config)?;
//   todo!()
// }
