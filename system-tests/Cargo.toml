[package]
name = "system-tests"
version.workspace = true
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap.workspace = true
unimarkup-core = { path = "../core/" }
shlex = "1.1.0"
